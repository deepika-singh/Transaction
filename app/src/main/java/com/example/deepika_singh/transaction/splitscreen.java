package com.example.deepika_singh.transaction;

import android.content.DialogInterface;
import android.content.Intent;
import android.preference.DialogPreference;
import android.support.v7.app.AlertDialog;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.ImageView;


public class splitscreen extends AppCompatActivity {

    //private static ImageView image_sbm;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_splitscreen);

        //public void onButtonClickListener(){

        //image_sbm = (ImageView) findViewById(R.id.imageView4);
        //image_sbm.setOnClickListener(
                //new View.OnClickListener(){
                    //@Override
                   // public void onClick(View v){
                        //AlertDialog.Builder a_builder = new AlertDialog().Builder(this);
                       // a_builder.setMessage("Tap to Enter Amount").setCancelable(false).setPositiveButton("tap", new DialogInterface.OnClickListener() {
                           // @Override
                            //public void onClick(DialogInterface dialog, int which) {

                            }
                        //}{
                       // })

                    }
                //}
        //)
   // }




        //ImageView i = (ImageView) findViewById(R.id.imageView4);
        //i.setOnClickListener(new View.OnClickListener(){
            //@Override
           // public void onClick(View v){
               /// startActivity(new Intent(splitscreen.this,tap.class));
           // }
        //});


   // }
//}
